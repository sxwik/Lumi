# RFC-0003: Lumi Name Service (LNS) Specification

**Category:** Core Standard  
**Status:** Draft  

---

## 1. Abstract
LNS resolves `.lumi` domain names (e.g. `docs.lumi`, `search.lumi`) to TCP network endpoints (`IP:Port`) without relying on traditional DNS or public web CAs.
