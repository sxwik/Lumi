# RFC-0004: LPKG Package Standard

**Category:** Package Standard  
**Status:** Draft  

---

## 1. Abstract
The `.lpkg` single-file archive format packages site metadata, LumiML markup (`index.lml`), and static binary assets into a single binary envelope for instant deployment.
